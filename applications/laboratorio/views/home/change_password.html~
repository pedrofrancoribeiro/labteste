{{extend 'layoutindex.html'}}

{{block jumbotron}}
{{end}}

{{block main}}
    <div class="container">

    <form class="col-md-6 col-md-offset-3 form-formulario" action="#" enctype="multipart/form-data" method="post">
    
 		 <img src="{{=URL('static','imagens',args='labinstru.png')}}" alt="icone-labinstru" class="img-responsive center-block">   	
 
        <fieldset id="trocaSenha" class="col-md-12">
            <legend>Redefinir Senha</legend>
            <p>
                <label class="form-label-user" for="senhaAntiga">Senha antiga</label>
                <input class="form-input-user form-control" type="password" id="senhaAntiga" name="old_password" required>
                <span class="hint">informe a senha atual</span>
            </p>

            <p>
                <label class="form-label-user" for="senhaAntiga">Nova senha</label>
                <input class="form-input-user form-control" type="password" id="senhaAntiga" name="new_password" required>
                <span class="hint">informe a nova senha</span>
            </p>

            <p>
                <label class="form-label-user" for="senhaAntiga">Verifica senha</label>
                <input class="form-input-user form-control" type="password" id="senhaAntiga" name="new_password2" required>
                <span class="hint">repita a nova senha</span>
            </p>
        </fieldset>
        <button class="btn btn-success btn-aux">Redefinir Senha</button>
    {{=change_password.custom.end}}

    </div> <!-- /container -->
{{end}}